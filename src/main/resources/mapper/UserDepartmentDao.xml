<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.curtis.talent_recruitment.department.dao.UserDepartmentDao">

    <select id="getList" resultType="com.curtis.talent_recruitment.department.entity.UserDepartment">
        select
            id, sUserID, sDepartmentID
        from
            t_user_department
    </select>

    <select id="getDetail" resultType="com.curtis.talent_recruitment.department.entity.UserDepartment">
        select
            id, sUserID, sDepartmentID
        from
            t_user_department
        where
            id = #{id}
    </select>

    <insert id="add">
        insert into
            t_user_department
        (
            id, sUserID, sDepartmentID
        )
        values
        (
            #{id},#{sUserID},#{sDepartmentID}
        )
    </insert>

    <delete id="delete">
        delete from
            t_user_department
        where
            id = #{id}
    </delete>

    <update id="update">
        update
            t_user_department
        set
            sUserID = #{sUserID},
            sDepartmentID = #{sDepartmentID}
        where
            id = #{id}
    </update>

    <!-- 根据条件查询统计信息 -->
    <select id="getCount" resultType="int">
        select
            count(1)
        from
            t_user_department
        where
            1=1
        <if test="sDepartmentID != null and sDepartmentID !=''">
            and sDepartmentID = #{sDepartmentID}
        </if>
        <if test="sUserID != null and sUserID !=''">
            and sUserID = #{sUserID}
        </if>
    </select>

</mapper>